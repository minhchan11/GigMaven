package app;

/**
 * The enum Non moving violation.
 */
public enum NonMovingViolation implements Violation {
  /**
   * Parking violation non moving violation.
   */
  PARKING_VIOLATION,
  /**
   * Paper work issues non moving violation.
   */
  PAPER_WORK_ISSUES,
  /**
   * Problems with the vehicle non moving violation.
   */
  PROBLEMS_WITH_THE_VEHICLE
}
